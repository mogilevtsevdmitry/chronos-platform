# ⏳ Chronos Platform

> Веб-платформа исторической хронологии с астрономическим движком

**Live:** https://72.56.112.248.nip.io/chronos

---

## Что это

Chronos — платформа для работы с историческими событиями и хронологиями, использующая Julian Day Number (JDN) как универсальный временной якорь. Поддерживает даты от 10 000 лет до н.э. до 10 000 н.э.

## Возможности

- 📅 **Таймлайн событий** — визуальный просмотр исторических событий по эпохам
- 🔢 **Конвертер дат** — перевод григорианских/юлианских дат в JDN и обратно
- 🔭 **Астрономический движок** — алгоритмы Жана Мюса (Jean Meeus) для точных астрономических вычислений
- 🌐 **REST API** — публичный API для интеграций

## Стек

| Слой | Технология |
|------|-----------|
| Backend API | NestJS + TypeScript |
| Frontend | Next.js 14 + React 18 |
| База данных | PostgreSQL + Prisma ORM |
| Деплой | Docker Compose + nginx |
| Даты | Julian Day Number (BIGINT) |

## Быстрый старт

```bash
git clone https://github.com/mogilevtsevdmitry/chronos-platform
cd chronos-platform

# Установить зависимости
npm install

# Настроить окружение
cp apps/api/.env.example apps/api/.env
# Отредактировать DATABASE_URL

# Применить схему БД
npx prisma db push --schema=apps/api/prisma/schema.prisma

# Запустить
npm run dev
```

## API

**Base URL:** `https://72.56.112.248.nip.io/chronos/api`

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/events` | Список событий (с пагинацией) |
| GET | `/eras` | Список исторических эпох |
| GET | `/calendar/convert?date=YYYY-MM-DD` | Конвертация даты в JDN |

## Архитектура

```
chronos-platform/
├── apps/
│   ├── api/          # NestJS backend (порт 3002)
│   │   ├── src/
│   │   │   ├── events/       # события
│   │   │   ├── eras/         # эпохи
│   │   │   ├── calendar/     # JDN конвертер + алгоритмы
│   │   │   └── common/       # filters, pipes
│   │   ├── prisma/           # схема БД
│   │   └── Dockerfile
│   └── web/          # Next.js frontend (порт 3003)
├── nginx/            # HTTPS + rate limiting
└── docker-compose.yml
```

## Roadmap

- [ ] Парсер данных из Wikidata SPARQL
- [ ] Фильтрация по эпохам и датам
- [ ] Поиск по событиям
- [ ] Режим анализа хронологий
- [ ] Спорные хронологии (расширение)

## Лицензия

MIT
